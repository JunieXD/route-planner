---
name: route-planner
description: "规划中国境内由铁路、地铁、公交和步行组成的完整行程，比较建议出发时间、班次时刻、到达时间、费用、总耗时、换乘和风险。适用于跨城与市内交通规划，不用于景点游览安排或购票。"
---

# Route Planner

根据用户提供的起点、终点和可选途经点，规划有可靠数据依据的完整行程。查询公开交通数据时，应先使用本技能提供的脚本，无需为了查询而先打开网页。

## 规划流程

1. 提取日期、人数、起点、终点、途经点、期望出发时段或最晚到达时间、席别、允许的交通方式，以及“最快、最省、均衡、少换乘”等偏好。日期已知但全天班次很多时，时间范围会显著影响结论，应向用户确认或明确说明采用的时段；其余缺失信息可以作出合理假设，但必须注明。
2. 区分必须满足的条件和用于比较方案的偏好。先淘汰不符合预算、到达时限、席别、无障碍出行或途经要求的方案，再比较其余方案。
3. 跨城铁路先运行 `scripts/rail_12306.py direct`。若没有直达车，或换乘更符合用户偏好，再运行 `transfer`：它先在设定范围内搜索换乘枢纽，然后读取相关列车的完整经停站，查找两趟列车都停靠且可以换乘的车站，并重新查询各段票价和余票。检查 `search_coverage` 中列出的枢纽、查找出的换乘站、查询次数上限和提前停止情况。查询范围有限时，只能写“在已查询方案中费用最低”或“在已查询方案中用时最短”，不能声称已经找到所有车次中费用最低或用时最短的方案。若用户给出具体车站，保留该车站；若只给城市，应比较该城市实际可用的多个到发站。
4. 保留费用、时长和到达站有明显差异的铁路方案，为每个方案补齐出发地到车站、车站到目的地的路段后再排名。铁路部分费用最低，不代表完整行程费用最低：
   - 上海地铁使用 `scripts/shanghai_metro.py`，无需浏览器或密钥。
   - 其他城市优先使用 `scripts/amap_transit.py`；它依次从 `AMAP_MAPS_API_KEY`、macOS 钥匙串或 Windows 凭据管理器读取高德 Web 服务密钥。
   - 没有可用接口时，先说明缺少哪些数据，再使用官方网页或地图查询；未知票价或耗时不得写成精确数值。
5. 向路线接口提交地址前，先校验地理编码。把用户给出的省、市、区县、校区、车站和门牌信息转成 `--origin-*` / `--destination-*` 约束；若结果所属地区不符，或具体地点只解析到省、市、区县中心点，应停止使用该结果，改用完整地址或可靠坐标。不能为了获得查询结果而默认接受精度不足的坐标。
6. 途经点按用户给定顺序拆成连续路段。将换乘步行、候车、铁路进站预留和用户要求的停留时间分别计入，避免只相加车内时间。若用户给出最晚到达时间，应倒推出最晚应从起点出发的时间，并将到达期限列为必须满足的条件；若截止时间缺失且会改变推荐，可以给出条件结论，或只询问这一项关键信息。
7. 多个方案按 [路线数据结构](references/route-schema.md) 整理，再运行 `scripts/rank_routes.py`。先标明费用、时间和路段是否完整，再保留各有实际优势的帕累托前沿方案；表格行数应由有效方案数量决定。

选择数据来源或处理接口异常时，读取 [数据来源说明](references/providers.md)。组合三个以上路段、计算多人费用合计或解释总耗时时，读取 [路线数据结构](references/route-schema.md)。准备最终答复时，读取 [简洁输出格式](references/output-format.md)。

当高德脚本提示缺少凭据，或用户询问如何保存、更新、检查或删除密钥时，读取 [高德密钥设置说明](references/amap-key-setup.md)。不要让用户把密钥粘贴到聊天中，也不要输出已经保存的密钥。

## 时间和费用如何计算

- “总耗时”默认指从实际起点到最终目的地的全部时间，包括前后接驳、候车、进站预留、乘车、换乘和步行。铁路或地铁的运行时间应另外列出。
- 使用 `rank_routes.py` 整理组合方案后，检查其生成的 `door_to_door_duration_minutes`、`scheduled_span_minutes`、`in_vehicle_minutes`、`waiting_minutes`、`checkin_buffer_minutes` 和 `local_connection_minutes`。起点到首站的接驳时间未知时，完整行程耗时必须标为不完整，并写成“目前可计算的耗时 + 住处到首站的时间”；不能把单段车程或铁路行程跨度写成总耗时。
- 每个方案至少计算三个关键时刻：从实际起点出发的“建议出发时间”、主要班次的“发车时间”、抵达最终目的地的“预计到达时间”。到达火车站或地铁站的时间不能代替最终到达时间。
- 固定班次使用官方时刻；公交、地铁没有可核实的具体班次时，不要给出无法核实的精确发车时间，只给预计上车时间或时间范围。跨日到达必须标注“次日”和日期。
- 用户没有特别说明时，暂按提前 30 分钟到达铁路车站计算，并把这一点列为假设；大型车站、节假日、首次到站或需要办理额外事项时，可以增加预留时间。
- 若市内路线查询以车站入口为起点，应另计下车后走到地铁闸机或站外接驳点的时间；没有可靠数据时给出范围，不要给出无法核实的精确数值。
- 报告每人费用和按出行人数计算的合计费用。未知或尚未计入的费用必须单独说明，不能按 0 元计算。
- 只有 `price_complete=true` 时，才能写“总费用”或标为“费用最低”；否则写“已知费用 ¥x，另有某段费用待定”。只有 `time_complete=true` 且没有遗漏路段时，才能写完整行程的“总耗时”或标为“用时最短”。
- 票价、余票和运行状态都要注明查询时间。超出铁路预售期时，只能把同一星期几的列车时刻或历史票价作为参考，并清楚标注，不能称为目标日期的实时结果。
- 清晨和深夜出行时，必须检查 `service_window_verified`。无法确认首末班时间时，精确到达时间只能写成“若该时段仍有车，最早约 HH:MM”，并标注“首末班待确认”；不得仅根据常规行程耗时认定该时段仍有车。

## 用户通常还会在意

- 接驳和换乘是否可行：站内外步行时间、步行距离、跨站换乘、时间最紧的一次换乘还剩多少余量，以及错过固定班次后的影响。
- 方案是否稳妥：是否有余票或已经开售、数据更新时间、公交班次是否稀疏、首末班限制、浮动价格和尚未计入的费用。换乘可靠性和旅途负担应分开说明：较长的候车时间通常更不容易误车，但也会增加疲劳。只在有事实依据时标注“稳、一般、赶、待确认”。
- 旅途负担：换乘次数、最长连续步行、行李、老人、儿童、轮椅或电梯需求。仅在用户提出或路线差异明显时展开，避免默认堆满表格。
- 费用包含哪些项目：各段费用、每人合计、全体同行人员的合计费用，以及出租车浮动价格、优惠资格或尚未定价的路段。
- 夜间乘车舒适度：最便宜的普通座席方案和最便宜的卧铺方案应分开比较；使用 `cheapest-available`、`seat-only`、`sleeper-required` 或指定席别，票价排序与舒适度比较必须分开。
- 是否实际可行：默认把当前有票的方案与仅有票价但已经售罄的方案分开。只有 `inventory_confirmed=true` 的方案才能标为推荐、最快或最省；售罄卧铺的票价可以列作参考，但不能作为实际可行的方案推荐。

## 输出要求

先用一句话说明推荐方案和理由，再用一张简洁表格展示三至五个确有差异且实际可行的方案。每行必须让用户直接看到“建议出发 → 主要班次发车 → 最终到达”、完整行程总耗时、每人费用和同行者的费用合计、换乘与步行情况，以及稳妥程度或重要提醒。起点只精确到城市或区县时，写“应在 HH:MM 前到站，另加住处到车站的时间”，不要编造具体出发时间。开头不要先写长篇分析，也不要在各方案中重复相同信息。

表格下方只展开推荐方案的分段时间线，以及一个会明显影响选择的备选方案；其余细节按需提供。数据来源、查询时间、假设和各方案共有的不确定信息，应集中写在表格下方的简短说明中。具体排版遵循 [简洁输出格式](references/output-format.md)。

说明推荐方案与“最快”和“最省”方案相比，多花多少时间或费用。若用户只说明偏好但没有给出权重，可以保留“综合推荐、最快、最省、少换乘”等标签；不要把主观权衡写成唯一的客观最优方案。同一方案获得多个标签时，应合并显示，不要重复列出。

## 脚本

```bash
python3 scripts/rail_12306.py station --name "上海"
python3 scripts/rail_12306.py direct --date 2026-09-04 --from "杭州东" --to "上海" --train-types G,C --depart-after 08:00 --depart-before 11:00 --sort duration --limit 10
python3 scripts/rail_12306.py transfer --date 2026-09-10 --from "宁都" --to "上海" --max-transfers 1 --seat-policy cheapest-available --sort price --limit 10
python3 scripts/rail_12306.py sale-time --station "杭州东"

python3 scripts/shanghai_metro.py route --date 2026-09-11 --from "上海南站" --to "金沙江路" --limit 3

python3 scripts/amap_credentials.py status
python3 scripts/amap_transit.py transit --origin "杭州东站" --destination "西湖文化广场" --origin-city "杭州" --origin-district "上城" --destination-city "杭州" --destination-district "拱墅"
python3 scripts/amap_transit.py walk --origin "金沙江路地铁站" --destination "上海市普陀区中山北路3663号" --origin-city "上海" --destination-city "上海" --destination-district "普陀"

python3 scripts/rank_routes.py routes.json --optimize balanced --party-size 2 --limit 5
```

脚本输出 JSON，供模型组合和解释；不要直接向用户展示原始 JSON。

## 边界

- 只查询公开交通信息，不索取或保存 12306 账号、Cookie、身份证、联系人或支付信息。
- 不执行购票、候补、选座、改签、退票或支付。
- 不因两个车站属于同一地级行政区就认定可以跨站换乘。使用 `--allow-cross-station` 时，仍须同时提供 `--cross-station-rules`；文件中的每条接驳都必须写明经过核实的耗时、费用和来源。没有真实接驳数据时，应排除相应方案。
- 不绕过验证码、限流或访问控制。短暂失败可有限重试；持续失败时报告上游状态。
- 高德请求顺序执行；适配器会跨进程节流并对限流、429 和 5xx 有限退避。不要并发启动多个高德查询，也不要无限重试。
- 本技能只规划交通；除非用户另行要求，不扩展到酒店、景点或完整旅游日程。
